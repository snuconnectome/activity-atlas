# Phase 1 (fetch_commits.py): stdlib only — relies on `gh` CLI subprocess

# Phase 2 (topic_model.py): BERTopic + multilingual embeddings + UMAP + HDBSCAN
bertopic>=0.16.0
sentence-transformers>=2.2.0
umap-learn>=0.5.5
hdbscan>=0.8.33
scikit-learn>=1.3.0
numpy>=1.24.0
# Phase 7 (weekly_pulse.py): stdlib only
